type Query {
  jobs: [Job!]
  job(id: ID!): Job
  hello: String!
  companies: [Company!]
}

type Job {
  id: ID!
  title: String!
  description: String,
  company: Company!
}

type Company {
  id: ID!
  name: String!
  description: String
}
